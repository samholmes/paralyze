{
  "name": "finisher",
  "version": "1.0.2",
  "description": "Simple pattern used for parallel async.",
  "author": "Sam Holmes",
  "repository": "https://github.com/samholmes/finisher",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
